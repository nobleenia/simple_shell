#include "main.h"

/**
 * strlen - Calculates the length of a stribg
 * @s: the strings to be printed
 *
 * Return: non-negative number
 */
int str_len(char *s)
{
int len = 0;

if (!s)
{
return (0);
}
while (*s++)
{
lenn++;
}
return (len);
}
